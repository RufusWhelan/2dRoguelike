using System.Net.Http.Headers;
using UnityEngine;

public class Intentions : MonoBehaviour
{
    private string[] idleIntentions = { "stationary", "moving" };
    private string[] pursuitIntentions = { "approach", "attack", "recharge", "Retreat" };
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
